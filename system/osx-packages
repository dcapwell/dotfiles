reattach-to-user-namespace    // used to copy from tmux to clipboard
fswatch                       // used to execute commands when files get updated (recompile)
maven                         // a java build system
gradle                        // a java build system
libtool                       // needed or autogen doesn't work
gnu-tar                       // because f mac
csshX                         // Login to multiple hosts at a time
cmake                         // for building native code
fx                            // JSON interactive tool
gawk                          // better version of awk
